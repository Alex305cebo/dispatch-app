import { redirect } from 'next/navigation'

/**
 * Заведение груза из рейт-кона живёт на «Новом грузе», и только там.
 *
 * Экранов было два: этот и /loads/new. Оба принимали PDF, оба звали одно и то же
 * распознавание, оба заканчивались одной формой — разница была лишь в том, что здесь
 * показывались цитаты из документа. Цитаты переехали туда (components/rc-evidence.tsx),
 * а адрес остаётся живым: он в закладках и в старых письмах.
 */
export default function Page() {
  redirect('/loads/new')
}
