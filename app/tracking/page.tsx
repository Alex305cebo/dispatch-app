import { redirect } from 'next/navigation'

/**
 * «Трекинг» слился с «Траками»: карта, счётчики и список живут теперь на одной
 * странице с экономикой и документами — см. app/trucks/fleet-board.tsx.
 *
 * Адрес остаётся и уводит на новое место, а не отдаёт 404: он в закладках, в
 * письмах и в истории браузера у всех, кто пользовался приложением до слияния.
 */
export default function Page() {
  redirect('/trucks')
}
