/**
 * Слово, которое печатают вместо пароля перед удалением.
 *
 * Отдельным модулем, потому что нужно обеим сторонам: форма подтверждения в
 * браузере сверяет введённое, сервер проверяет то же самое ещё раз. Класть его в
 * lib/session.ts нельзя — тот помечен 'server-only' и в клиентский бандл не идёт.
 *
 * Английское и заглавными нарочно: его нельзя набрать случайно, не переключив
 * раскладку и не подумав.
 */
export const DELETE_WORD = 'DELETE'
